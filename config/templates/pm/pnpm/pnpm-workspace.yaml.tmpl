{{- if .Layout.IsMonorepo}}
packages:
  - "apps/*"
  - "packages/*"
{{end}}
strictDepBuilds: true
minimumReleaseAge: 20160
trustPolicy: no-downgrade
trustPolicyExclude:
  - chokidar
  {{- if .Base.IsNuxt}}
  - tinyclip
  {{- end}}
  {{- if or (ne .Backend "none") (ne .ORM "none")}}
  - undici-types
  {{- end}}
overrides:
  semver: "^7.7.1"
allowBuilds:
  {{- if .Base.IsNuxt}}
  "@parcel/watcher": true
  {{- end}}
  esbuild: true
  sharp: true
  # postinstall only swaps Vue 2/3 shims; the Vue 3 default is already right
  vue-demi: false
  {{- if or (eq .Deployment "cloudflare-pages") (eq .Deployment "cloudflare-workers")}}
  workerd: true
  {{- end}}
  {{- if and (or (eq .ORM "drizzle") (eq .ORM "prisma")) (or (eq .Database "sqlite") (eq .Database "none"))}}
  better-sqlite3: true
  {{- end}}
  {{- if eq .ORM "prisma"}}
  "@prisma/client": true
  "@prisma/engines": true
  prisma: true
  {{- end}}