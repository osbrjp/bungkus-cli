---
description: Format and lint the codebase, fixing what can be auto-fixed
---
Run `{{ .PM }} format` to format and lint the codebase. Fix any remaining issues
the formatter/linter can't auto-fix, then re-run until clean. Report what changed
in one or two lines — don't paste the full diff.
